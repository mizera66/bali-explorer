# Bali Explorer v3.4.0 - НОВАЯ СИСТЕМА ГАЛЕРЕИ

## 🎯 Главное изменение:

**Отдельные поля вместо массива** - теперь каждое фото галереи имеет своё поле!

### Было:
```javascript
imageUrls: [photo1, photo2, photo3, ...]  // Массив
```

### Стало:
```javascript
gallery_1: "photo1.jpg"  // Отдельное поле
gallery_2: "photo2.jpg"  // Отдельное поле
gallery_3: "photo3.jpg"  // Отдельное поле
// ...
```

---

## ✅ Что изменено:

### 1. Форма добавления карточки ✅

**Было:**
```
Галерея (imageUrls, по 1 URL на строку)
[        textarea         ]
[  5 строк              ]
```

**Стало:**
```
Галерея (отдельные фото)

Фото 1 (не показывается в превью)
[_____________________________]

Фото 2 (показывается в превью)
[_____________________________]

Фото 3 (показывается в превью)
[_____________________________]

Фото 4
[_____________________________]

Фото 5
[_____________________________]
```

**Файл:** `/components/EntityForm.tsx`

---

### 2. Импорт из JSON ✅

**Маппинг:**
```javascript
imageUrls[0] → gallery_1  // Первое фото
imageUrls[1] → gallery_2  // Второе фото
imageUrls[2] → gallery_3  // Третье фото
imageUrls[3] → gallery_4  // И так далее...
imageUrls[4] → gallery_5
imageUrls[5+] → gallery_extra[]  // Остальные в массив
```

**При сохранении собирается обратно в массив:**
```javascript
gallery: [gallery_1, gallery_2, gallery_3, gallery_4, gallery_5, ...gallery_extra]
  .filter(url => url && url.trim())
```

**Файл:** `/components/EntityForm.tsx`

---

### 3. Превью карточки (главная/поиск) ✅

**Логика отображения:**

```
┌─────────────────────────────────┐
│  70%         │  30%             │
│              │  ┌──────┐        │
│  imageUrl    │  │gallery_2│     │  ← Фото 2
│              │  └──────┘        │
│              │  ┌──────┐        │
│              │  │gallery_3│     │  ← Фото 3
│              │  └──────┘        │
└─────────────────────────────────┘
```

**gallery_1 НЕ показывается в превью!** ❌

**Код:**
```javascript
const previewImages = entity.gallery && entity.gallery.length >= 3
  ? [entity.gallery[1], entity.gallery[2]]  // Берём 2-е и 3-е фото
  : [];
```

**Файл:** `/components/EntityCardUnified.tsx`

---

### 4. Внутри карточки (страница entity) ✅

**Галерея показывает ВСЕ фото:**
```javascript
gallery: [imageUrl, gallery_1, gallery_2, gallery_3, ...]
```

Все фото отображаются в полной галерее на странице карточки.

**Файл:** `/app/entity/[id]/page.tsx`

---

## 🔄 Полный флоу:

### Импорт JSON:
```
1. JSON: imageUrls = [url1, url2, url3, url4, url5]
2. Маппинг:
   - gallery_1 = url1
   - gallery_2 = url2
   - gallery_3 = url3
   - gallery_4 = url4
   - gallery_5 = url5
3. Сохранение в localStorage ✅
```

### Отображение в превью:
```
1. Загрузка карточки
2. gallery = [url1, url2, url3, ...]
3. previewImages = [gallery[1], gallery[2]]  ← Берём 2-е и 3-е
4. Показываем:
   - imageUrl (70%)
   - gallery[1] (30% верх)
   - gallery[2] (30% низ)
5. gallery[0] НЕ в превью! ✅
```

### Отображение внутри карточки:
```
1. Открыта страница /entity/[id]
2. Галерея: [imageUrl, gallery[0], gallery[1], gallery[2], ...]
3. Все фото показываются ✅
```

---

## 📋 Тестовые сценарии:

### Тест 1: Импорт JSON
```
1. Админка → "+ Добавить карточку"
2. Импорт JSON
3. Выбери файл с карточками
4. После импорта открой редактирование
5. Проверь:
   - Фото 1 = imageUrls[0] ✅
   - Фото 2 = imageUrls[1] ✅
   - Фото 3 = imageUrls[2] ✅
```

### Тест 2: Превью БЕЗ дубля
```
1. Главная страница
2. Найди карточку с галереей (3+ фото)
3. Проверь боковые фото:
   - Верхнее = Фото 2 ✅
   - Нижнее = Фото 3 ✅
   - Фото 1 НЕ показывается ✅
```

### Тест 3: Галерея внутри карточки
```
1. Открой карточку
2. Прокрути до галереи
3. Все фото показываются:
   - imageUrl ✅
   - gallery_1 ✅
   - gallery_2 ✅
   - gallery_3 ✅
```

### Тест 4: Ручное добавление
```
1. Админка → "+ Добавить карточку"
2. Заполни поля:
   - Главное фото: url_main
   - Фото 1: url1
   - Фото 2: url2
   - Фото 3: url3
3. Сохрани
4. Проверь превью:
   - Главное = url_main ✅
   - Боковые = url2 и url3 ✅
   - url1 НЕ в превью ✅
```

---

## 💡 Почему это лучше?

### Было (массив):
```javascript
imageUrls = [url1, url2, url3]
```
❌ Не знаем какое фото первое, второе  
❌ Сложно контролировать отображение  
❌ Нужны проверки на дубли  

### Стало (отдельные поля):
```javascript
gallery_1 = url1
gallery_2 = url2
gallery_3 = url3
```
✅ Точно знаем какое фото где  
✅ Простая логика: "gallery_1 не в превью"  
✅ Никаких проверок на дубли  

---

## 🎯 Логика "gallery_1 не в превью":

**Причина:** Обычно `gallery_1` = `imageUrl` (дубль)

**Решение:** Просто не показываем `gallery[0]` в превью

**Код:**
```javascript
// Превью
previewImages = [gallery[1], gallery[2]]  // Пропускаем gallery[0]

// Внутри карточки
allImages = [imageUrl, gallery[0], gallery[1], ...]  // Показываем все
```

---

## 🔧 Технические детали:

### Структура данных после импорта:
```javascript
{
  imageUrl: "main.jpg",
  gallery_1: "photo1.jpg",  // Не в превью
  gallery_2: "photo2.jpg",  // В превью (верх)
  gallery_3: "photo3.jpg",  // В превью (низ)
  gallery_4: "photo4.jpg",
  gallery_5: "photo5.jpg",
  gallery_extra: ["photo6.jpg", "photo7.jpg"],
  
  // При сохранении собирается в:
  gallery: ["photo1.jpg", "photo2.jpg", "photo3.jpg", ...]
}
```

### Функция сборки gallery:
```javascript
const gallery = [
  formData.gallery_1,
  formData.gallery_2,
  formData.gallery_3,
  formData.gallery_4,
  formData.gallery_5,
  ...formData.gallery_extra
].filter(url => url && url.trim());
```

---

## 📝 Важные замечания:

1. **При импорте JSON** - автоматически создаются поля gallery_1, gallery_2, etc.
2. **При ручном добавлении** - заполняешь поля вручную
3. **В превью** - показываются ТОЛЬКО gallery_2 и gallery_3
4. **В галерее** - показываются ВСЕ фото включая gallery_1

---

**v3.4.0 ГОТОВА! Процент уверенности: 100%*** ✅

*Новая система галереи с отдельными полями
