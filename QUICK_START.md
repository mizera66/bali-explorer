# 🚀 BALI EXPLORER - БЫСТРЫЙ СТАРТ

## ✅ Что готово

Полнофункциональный MVP веб-сервиса с:

### Основные возможности
- ✅ Поиск и фильтры (район, тип, теги)
- ✅ Карточки мест/сервисов/специалистов
- ✅ Система доверия (confirm/report)
- ✅ Гайды с практическими инструкциями
- ✅ Админ-панель для управления базой (`/admin`)
- ✅ Адаптивный дизайн (mobile-first)
- ✅ Встроенная БД с mock-данными

### Технологии
- Next.js 14 (App Router)
- React 18
- TypeScript
- Tailwind CSS
- Lucide Icons

---

## 📥 Установка и запуск

### Шаг 1: Распаковать и перейти в папку

```bash
cd bali-explorer
```

### Шаг 2: Установить зависимости

```bash
npm install
```

### Шаг 3: Запустить в режиме разработки

```bash
npm run dev
```

Сайт откроется на **http://localhost:3000**

---

## 🗂️ Структура страниц

| Страница | URL | Описание |
|----------|-----|----------|
| Главная | `/` | Поиск + категории + популярное |
| Поиск | `/search?q=...` | Результаты поиска с фильтрами |
| Категория | `/category/service?tags=...` | Фильтр по типу и тегам |
| Карточка | `/entity/:id` | Детальная информация о месте |
| Гайды | `/guides` | Список практических гайдов |
| Гайд | `/guide/:id` | Отдельный гайд |
| Избранное | `/favorites` | Сохранённые карточки |
| Ещё | `/more` | Дополнительное меню |
| **Админ** | `/admin` | **Управление базой данных** |

---

## 🔧 Админ-панель

**URL:** http://localhost:3000/admin

### Возможности:
- ➕ Добавление новых карточек
- ✏️ Редактирование существующих
- 🗑️ Удаление (архивирование)
- 👁️ Просмотр статусов и рейтингов
- 🔍 Фильтрация по статусу (active/flagged/unverified)

**Важно:** В текущей версии админка не защищена паролем. Для продакшна добавь аутентификацию.

---

## 📊 База данных

### Текущая реализация (MVP)
- **Файл:** `data/db.ts`
- **Тип:** JSON в памяти
- **Данные:** 5 карточек + 2 гайда

### Структура Entity:
```typescript
{
  id: string
  type: 'place' | 'service' | 'specialist' | 'realtor'
  title: string
  short_description: string
  area: string  // Чангу, Убуд, etc
  contacts: { whatsapp?, instagram?, website? }
  price_level: 1-4  // $ to $$$$
  tags: string[]
  trust_score: 0-100
  status: 'active' | 'unverified' | 'flagged'
  image_url?: string
}
```

### Для продакшна
Мигрируй на PostgreSQL + Prisma:
1. Установи Prisma: `npm install prisma @prisma/client`
2. Создай схему в `prisma/schema.prisma`
3. Замени `lib/db-service.ts` на Prisma queries

---

## 🎨 Кастомизация

### Цвета (tailwind.config.js)
```js
primary: '#00A86B'    // Основной зелёный
trust: '#22C55E'      // Доверие
warning: '#F59E0B'    // Внимание
danger: '#EF4444'     // Опасность
```

### Шрифты (styles/globals.css)
```css
--font-display: 'Sora'  // Заголовки
--font-body: 'Inter'    // Текст
```

---

## 📱 Основные компоненты

### EntityCard (`components/EntityCard.tsx`)
Универсальная карточка для всех типов объектов.

**Props:**
- `entity`: Entity - объект из БД
- `detailed?`: boolean - расширенный вид

**Функции:**
- WhatsApp, карты, Instagram
- Trust score
- Confirm/Report

### Navigation (`components/Navigation.tsx`)
Нижняя навигация (sticky).

**Разделы:**
- Главная
- Избранное
- Гайды
- Ещё

---

## 🔄 API Routes

Все API в `app/api/`:

### Entities
```typescript
GET    /api/entities              // Список с фильтрами
GET    /api/entities/:id          // Одна карточка
POST   /api/entities              // Создать
PUT    /api/entities/:id          // Обновить
DELETE /api/entities/:id          // Удалить
```

**Фильтры (query params):**
- `type` - place/service/specialist
- `area` - район
- `tags` - теги через запятую
- `q` - поисковый запрос
- `popular=true` - топ по trust score

### Signals
```typescript
POST /api/signals
Body: { entity_id, type: 'confirm'|'report', reason? }
```

### Guides
```typescript
GET /api/guides                   // Все гайды
GET /api/guides/:id              // Один гайд
```

---

## 🚀 Деплой

### Vercel (1 клик)
1. Пуш в GitHub
2. Импорт в Vercel
3. Deploy

### Другие варианты
- Netlify
- Railway
- Render
- Docker

---

## ⚡ Производительность

### Оптимизации в коде:
- ✅ Skeleton loaders
- ✅ Ленивая загрузка изображений
- ✅ Анимации с CSS
- ✅ Минимум JS на клиенте

### Для продакшна добавь:
- Redis для кэширования
- CDN для картинок
- Service Worker для offline
- Сжатие изображений

---

## 📋 Чек-лист перед продакшном

- [ ] Миграция на PostgreSQL
- [ ] Добавить аутентификацию в админку
- [ ] Настроить rate limiting
- [ ] Добавить аналитику (Google Analytics)
- [ ] Оптимизировать изображения
- [ ] SEO (meta tags, sitemap)
- [ ] Обработка ошибок
- [ ] Логирование
- [ ] Backup БД
- [ ] SSL сертификат

---

## 🔥 Следующие шаги (V2)

### Функции пользователя
- [ ] Избранное с сохранением (localStorage → auth)
- [ ] Создание коллекций
- [ ] Предложение новых мест (pending → approve)

### Улучшение поиска
- [ ] Full-text search (PostgreSQL)
- [ ] Автодополнение
- [ ] История поисков

### Социальные функции
- [ ] Комментарии к карточкам
- [ ] Рейтинги от пользователей
- [ ] Фото от сообщества

### Партнёры
- [ ] Регистрация партнёров
- [ ] Редактирование своих карточек
- [ ] Статистика просмотров

---

## ❓ FAQ

**Q: Как добавить новые карточки?**
A: Через админку `/admin` или напрямую в `data/db.ts`

**Q: Как изменить цвета?**
A: В `tailwind.config.js` секция `colors`

**Q: Нужна ли база данных?**
A: Для MVP нет (JSON в памяти). Для прода - да (PostgreSQL).

**Q: Как защитить админку?**
A: Добавь middleware с проверкой JWT/session.

**Q: Можно ли запустить оффлайн?**
A: Да, но нужен Service Worker (добавь в V2).

---

## 📞 Поддержка

Если возникли вопросы - пиши мне, я помогу.

**Успехов с запуском! 🚀**
