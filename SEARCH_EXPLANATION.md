# 🔍 КАК РАБОТАЕТ ПОИСК НА ГЛАВНОЙ СТРАНИЦЕ

## 📍 Где находится:
- Главная страница → поле поиска сверху
- Файл: `/app/page.tsx`

---

## 🎯 ПО КАКИМ ПОЛЯМ ИЩЕТ:

Поиск проверяет **4 поля** у каждой карточки:

### 1. **title** (название)
Например: 
- "Zen Spa Ubud" ← ищет "zen", "spa", "ubud"
- "Bali Bike Rental" ← ищет "bike", "rental", "bali"

### 2. **short_description** (описание)
Например:
- "Традиционный балийский массаж" ← ищет "массаж", "балийский"
- "Аренда байков с доставкой" ← ищет "байк", "доставка"

### 3. **area** (район)
Например:
- "Чангу" ← ищет "чангу", "canggu"
- "Убуд" ← ищет "убуд", "ubud"

### 4. **tags** (теги)
Например:
- ["СПА", "Массаж", "Релакс"] ← ищет "спа", "массаж"
- ["Байки", "Скутеры", "Аренда"] ← ищет "байк", "скутер"

---

## 🔧 КАК РАБОТАЕТ ЛОГИКА:

### **Шаг 1: Преобразование в lowercase**
```javascript
const query = searchQuery.toLowerCase().trim();
// "Zen SPA" → "zen spa"
```

### **Шаг 2: Проверка каждого поля**
```javascript
// Название
entity.title.toLowerCase().includes(query)

// Описание
entity.short_description?.toLowerCase().includes(query)

// Район
entity.area?.toLowerCase().includes(query)

// Теги
entity.tags?.some(tag => tag.toLowerCase().includes(query))
```

### **Шаг 3: OR логика**
Если **ХОТЯ БЫ ОДНО** поле содержит запрос → карточка показывается

---

## 📝 ПРИМЕРЫ:

### Поиск: "spa"
Найдет:
- ✅ "Zen **Spa** Ubud" (в названии)
- ✅ "Балийский **СПА** центр" (в названии)
- ✅ Карточки с тегом "**СПА**"

Не найдет:
- ❌ "Massage Center" (нет слова spa)
- ❌ "Salon Beauty" (нет слова spa)

---

### Поиск: "чангу"
Найдет:
- ✅ Все карточки с `area: "Чангу"`
- ✅ "Coffee Shop in **Canggu**" (в названии)
- ✅ "Лучший байк в **Чангу**" (в описании)

---

### Поиск: "байк"
Найдет:
- ✅ "**Байк** Rental Canggu" (в названии)
- ✅ "Аренда **байков**" (в описании)
- ✅ Карточки с тегом "**Байки**"

---

## ⚙️ КОД:

```javascript
// /app/page.tsx
const filteredEntities = entities.filter((entity: Entity) => {
  if (!searchQuery) return true; // Показать все если пусто
  
  const query = searchQuery.toLowerCase().trim();
  
  // Поиск в 4 полях
  return (
    entity.title.toLowerCase().includes(query) ||
    entity.short_description?.toLowerCase().includes(query) ||
    entity.area?.toLowerCase().includes(query) ||
    entity.tags?.some(tag => tag.toLowerCase().includes(query))
  );
});
```

---

## 🎨 ОСОБЕННОСТИ:

### 1. **Регистронезависимый**
- "SPA" = "spa" = "Spa" = "sPa"

### 2. **Частичное совпадение**
- Поиск "массаж" найдет "**массаж**", "**Массаж**ист", "Тайский **массаж**"

### 3. **Мгновенный**
- Работает на клиенте, нет задержки
- Обновляется при каждом вводе символа

### 4. **Пустой запрос**
- Если поле пустое → показываются ВСЕ карточки

---

## 🚀 МОЖНО УЛУЧШИТЬ:

### **Вариант 1: Поиск по нескольким словам**
```javascript
// Поиск "spa ubud" найдет карточки с "spa" И "ubud"
const words = query.split(' ');
return words.every(word => 
  entity.title.toLowerCase().includes(word) ||
  entity.area?.toLowerCase().includes(word)
);
```

### **Вариант 2: Приоритет результатов**
```javascript
// Сначала показать совпадения в названии, потом в описании
entities.sort((a, b) => {
  const aTitle = a.title.toLowerCase().includes(query);
  const bTitle = b.title.toLowerCase().includes(query);
  if (aTitle && !bTitle) return -1;
  if (!aTitle && bTitle) return 1;
  return 0;
});
```

### **Вариант 3: Fuzzy search (нечеткий поиск)**
```javascript
// "massage" найдет "masage" (с опечаткой)
import Fuse from 'fuse.js';
```

---

## ✅ ИТОГО:

**Поиск работает просто:**
1. Вводишь запрос
2. Проверяются: название, описание, район, теги
3. Показываются карточки где ЕСТЬ совпадение
4. Регистр не важен
5. Частичные совпадения работают

**Логика: OR (хотя бы одно поле)**
